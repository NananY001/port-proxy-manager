
> 需要以管理员身份运行，脚本会自动提权。

运行脚本后按菜单操作：

- **[1]** 查看当前所有转发规则
- **[2]** 添加规则，依次输入本机端口、目标 IP（虚拟机 IP）、目标端口
- **[3]** 按本机端口号删除对应规则

以 VMware 虚拟机为例，将宿主机 `8080` 端口转发到虚拟机 `192.168.100.128:80`：

```
Local Port : 8080
Target IP  : 192.168.100.128
Target Port: 80
```

之后访问宿主机 `127.0.0.1:8080` 即可到达虚拟机的 80 端口。
